#include "main.h"
#include <stdio.h>

/**
 *int _strlen - returns length of a string
 *@s: string
 *Return: length of string
 */

int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)
	return (index);
}
