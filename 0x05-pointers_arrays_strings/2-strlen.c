#include "main.h"
#include <stdio.h>

/**
 *_strlen - checks the returns length of a string
 *@s: char input
 *Return: length of string
 */

int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)
	return (index);
}
